import React, { Component } from 'react';
/*
Interface with user to generate items (select options, min max value, ect)
regenerate display based on navbar selection (active generator perhaps?)
*/

class CreatorWidget extends Component {
    state = {  } 
    render() { 
        return (
            bananas
        );
    }
}
export default CreatorWidget;